version: "3.9"

services:
  db:
    image: postgres:16
    container_name: proc_mvp_db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: proc_mvp
    ports:
      - "5433:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

  # server:
  #   build:
  #     context: ./server
  #   container_name: proc_mvp_server
  #   env_file:
  #     - ./server/.env.example
  #   environment:
  #     # Override DB host to point at the db service
  #     DATABASE_URL: postgres://postgres:postgres@db:5432/proc_mvp?schema=public
  #     NODE_ENV: development
  #     PORT: 3001
  #     CLIENT_URL: http://localhost:5173
  #   depends_on:
  #     - db
  #   ports:
  #     - "3001:3001"

  # client:
  #   build:
  #     context: ./client
  #   container_name: proc_mvp_client
  #   environment:
  #     VITE_API_BASE_URL: http://localhost:3001
  #   depends_on:
  #     - server
  #   ports:
  #     - "5173:5173"

volumes:
  db_data:
